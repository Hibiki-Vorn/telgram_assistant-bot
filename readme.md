# Telegram Assistant Bot

This is my AI agent. It was deployed by cloudflare workers AI.

It can recognize the photo and call function. The model cannot call function so I realized my function calling in this project.

It can get current date, search by searXNG and calculate math expression by `math.js`.